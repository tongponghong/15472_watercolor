#include "S72_loader/S72.hpp"
#include "mathlibs/mat4.hpp"


#include <limits>
#include <optional>
#include <string>
#include <unordered_map>
#include <variant>
#include <vector>
#include <iostream>

// helper library to load in objs from scene

// S72 scene = S72();

// struct NodeInfo{
//     S72::Node *node;
//     mat4       nodeTransform;
// };

// void get_S72(std::string const &filename);

// void setup_tree(S72 scene);

// void traverse_scene(S72 &scene, std::vector< Tutorial::ObjectInstance > scene_objects);

// mat4 get_node_xform(S72::Node *node);

// void traverse_root(S72::Node *root, std::vector< Tutorial::ObjectInstance > scene_objects);

// std::vector< Tutorial::ObjectInstance > scene_objects;

//     // GOAL: get a list of std::vector< ObjectInstance > to keep track of objects and their parent transformations 
//     // then can use that list as the object instances read in to the GPU 





